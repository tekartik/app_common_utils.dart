name: tekartik_app_text
description: App text helpers (diacritics)
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.12.0

# Add regular dependencies here.
dependencies:
  collection: ">=1.19.1"
  diacritic: ">=0.1.6"
  tekartik_common_utils:
    git:
      url: https://github.com/tekartik/common_utils.dart


dev_dependencies:
  lints: ">=6.1.0"
  test: ">=1.31.0"
resolution: workspace
