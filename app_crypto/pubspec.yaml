name: tekartik_app_crypto
description: Crypto utilities
version: 0.2.4
homepage: https://www.example.com

publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  tekartik_common_utils:
    git:
      url: https://github.com/tekartik/common_utils.dart
    version: '>=0.10.7'
  meta: ">=1.18.0"
  # encrypt: '>=5.0.3' # <=5.0.3'
  # Temp aes issue limited to 5.0.1
  # encrypt: '<=5.0.3'
  tekartik_encrypt:
    git:
      url: https://github.com/tekartikdev/encrypt
      ref: main
  crypto:
  # Temp web compile issue
  pointycastle: '>=3.2.0-rc0'

dev_dependencies:
  dev_build: ">=1.1.7+3"
  test: ">=1.31.0"
  # downgrade testing
  file: '>=7.0.0'
  build_runner: ">=2.15.0"
  build_test: any
  build_web_compilers: ">=4.4.19"
  process_run: ">=1.2.1+1"

resolution: workspace
