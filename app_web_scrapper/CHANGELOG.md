## 0.1.0

- Initial version: `WebScrapper` (html, css, javascript imports, optional
  javascript/json string scan, external domains, depth, filter, cache/resume,
  content hooks), `WebScrapperCli` / `webScrapperMain` and the `web_scrapper`
  binary.
