name: tekartik_app_date
description: Date utilities
version: 0.1.6
publish_to: none
environment:
  sdk: ^3.12.0

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  tekartik_common_utils:
    git:
      url: https://github.com/tekartik/common_utils.dart
    version: '>=0.10.7'
dev_dependencies:
  lints: ">=6.1.0"
  test: ">=1.31.0"
resolution: workspace
